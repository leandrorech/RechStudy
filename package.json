{
  "name": "rechstudy",
  "private": true,
  "version": "1.0.0",
  "description": "RechStudy — test suite pilot (Vitest + jsdom) for pure calculation logic extracted from index.html.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
